[pytest]
addopts = --strict-markers
markers=
    smoke: this is for smoke test cases
    regression: this is for regression purpose
    login: this is for Login purpose

log_cli=true
log_level=INFO
log_format = %(asctime)s %(levelname)s %(message)s
log_date_format = %Y-%m-%d %H:%M:%S
